<h1>Songs of artist</h1>
<% @artist.songs.each do |song| %>
     <p><%= link_to song.track, song %></p>
 <% end %>

 <%= link_to "Home", root_path %>
 <%= link_to "Edit", edit_artist_path(@artist) %>
 <%= link_to "Delete", artist_path(@artist), method: :delete %>
